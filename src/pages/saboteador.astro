---
const sabotajes = [
  {
    texto: "Si quieres destruir tu hogar, deja que el orgullo gane cada discusión.",
    remate: "El amor no sobrevive donde nadie quiere perder."
  },
  {
    texto: "¿Quieres perder a alguien que te ama? Hazlo sentir pequeño cada vez que te hable.",
    remate: "El desprecio es silencioso, pero letal."
  },
  {
    texto: "Si quieres apagar tu brillo, compárate todos los días con vidas editadas.",
    remate: "Nadie florece midiéndose con filtros."
  },
  {
    texto: "Si quieres romper tu matrimonio, guarda silencio cuando algo te duele.",
    remate: "Lo que no se habla, se pudre."
  },
  {
    texto: "Si quieres arruinar tu paz, intenta agradar a todo el mundo.",
    remate: "La aprobación es una jaula elegante."
  }
];
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Modo Incómodo</title>

    <style>
      body {
        margin: 0;
        background: #0f0f0f;
        color: #f2f2f2;
        font-family: system-ui, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        text-align: center;
      }

      .card {
        max-width: 600px;
        padding: 40px;
      }

      h1 {
        letter-spacing: 3px;
        font-size: 1rem;
        color: #888;
      }

      .frase {
        font-size: 1.6rem;
        margin: 30px 0;
        line-height: 1.6;
        min-height: 120px;
      }

      .remate {
        font-size: 1rem;
        color: #aaa;
        margin-top: 20px;
        min-height: 40px;
      }

      button {
        margin-top: 30px;
        padding: 12px 24px;
        background: white;
        color: black;
        border: none;
        cursor: pointer;
        font-weight: bold;
        transition: 0.2s ease;
      }

      button:hover {
        transform: scale(1.05);
      }
    </style>
  </head>

  <body>
    <div class="card">
      <h1>MODO INCÓMODO</h1>

      <div id="frase" class="frase"></div>
      <div id="remate" class="remate"></div>

      <button id="btn">Dame un golpe de realidad</button>
    </div>

    <!-- Pasamos los datos de Astro al navegador correctamente -->
    <script type="application/json" id="data">
      {JSON.stringify(sabotajes)}
    </script>

    <script>
      const data = JSON.parse(
        document.getElementById("data").textContent
      );

      const fraseEl = document.getElementById("frase");
      const remateEl = document.getElementById("remate");
      const btn = document.getElementById("btn");

      function generar() {
        const random = data[Math.floor(Math.random() * data.length)];
        fraseEl.textContent = random.texto;
        remateEl.textContent = random.remate;
      }

      btn.addEventListener("click", generar);

      // Genera una al cargar
      generar();
    </script>
  </body>
</html>
