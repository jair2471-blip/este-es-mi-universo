---
import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'ANTONOV PRO | El Oráculo de los Líderes',
  description: 'Conciencia artificial para mentes de alto impacto.',
};
---

<Layout metadata={metadata}>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;600;700;900&display=swap');

    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { background: #000; font-family: 'Inter', sans-serif; color: white; overflow-x: hidden; }

    .app-container {
      min-height: 100vh;
      background: radial-gradient(ellipse at center, #0a0a0a 0%, #000000 100%);
      display: flex; align-items: center; justify-content: center;
      padding: 30px 20px; position: relative;
    }

    .app-container::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
      background-image: radial-gradient(1px 1px at 20% 30%, rgba(255, 215, 0, 0.1), transparent),
                        radial-gradient(1px 1px at 70% 60%, rgba(255, 255, 255, 0.05), transparent);
      background-size: 250px 250px; animation: particleFloat 40s linear infinite; pointer-events: none;
    }

    @keyframes particleFloat { from { background-position: 0 0; } to { background-position: 500px 1000px; } }

    .desktop-layout {
      width: 100%; max-width: 1400px;
      display: grid; grid-template-columns: 400px 1fr; gap: 40px;
      position: relative; z-index: 1;
    }

    .control-panel {
      background: rgba(13, 13, 13, 0.9); border: 1px solid rgba(255, 215, 0, 0.2);
      border-radius: 24px; padding: 40px; display: flex; flex-direction: column; gap: 25px;
      backdrop-filter: blur(12px); box-shadow: 0 40px 80px rgba(0,0,0,0.9); height: fit-content;
    }

    .brand { text-align: center; padding-bottom: 25px; border-bottom: 1px solid rgba(255, 215, 0, 0.1); }
    .brand h1 { font-family: 'Bebas Neue', cursive; font-size: 4.5rem; letter-spacing: 8px; margin: 0; text-shadow: 0 0 20px rgba(255, 215, 0, 0.2); }
    .brand p { color: #ffd700; font-size: 0.7rem; letter-spacing: 5px; font-weight: 900; text-transform: uppercase; margin-top: -5px; }

    textarea {
      background: #050505; border: 1px solid rgba(255, 215, 0, 0.2); border-radius: 15px;
      color: #fff; padding: 20px; font-size: 0.95rem; resize: none; min-height: 130px; width: 100%; transition: 0.3s;
    }
    textarea:focus { outline: none; border-color: #ffd700; box-shadow: 0 0 20px rgba(255, 215, 0, 0.1); }

    label { color: #555; font-size: 0.7rem; font-weight: 900; text-transform: uppercase; letter-spacing: 2.5px; margin-bottom: 8px; display: block; }

    .btn-primary {
      background: linear-gradient(135deg, #ffd700, #b8860b); color: #000; border: none;
      padding: 20px; border-radius: 15px; font-weight: 900; font-size: 1.1rem; cursor: pointer;
      text-transform: uppercase; letter-spacing: 3px; transition: 0.4s;
    }
    .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 15px 30px rgba(255, 215, 0, 0.3); }

    .btn-variant { background: transparent; color: #ffd700; border: 1px solid rgba(255, 215, 0, 0.4); padding: 14px; border-radius: 12px; font-weight: 700; cursor: pointer; margin-top: 10px; text-transform: uppercase; letter-spacing: 1px; }

    .preview-panel { background: #000; border: 1px solid rgba(255, 215, 0, 0.05); border-radius: 30px; padding: 30px; display: flex; align-items: center; justify-content: center; min-height: 650px; position: relative; }
    
    canvas { max-width: 100%; height: auto; border-radius: 8px; box-shadow: 0 60px 120px rgba(0,0,0,0.6); }

    @media (max-width: 1100px) { .desktop-layout { grid-template-columns: 1fr; } .control-panel { position: static; } }
  </style>

  <div class="app-container">
    <div class="desktop-layout">
      <aside class="control-panel">
        <div class="brand">
          <h1>ANTONOV</h1>
          <p>SISTEMA DE EXCELENCIA</p>
        </div>

        <div>
          <label>Confiesa tu bloqueo</label>
          <textarea id="userInput" placeholder="Habla con franqueza, líder..."></textarea>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
          <div>
            <label>Formato</label>
            <select id="formatSelect" style="width: 100%; background: #050505; color: #fff; border: 1px solid rgba(255,215,0,0.2); padding: 12px; border-radius: 10px;">
              <option value="post">FEED (1:1)</option>
              <option value="story">STORY (9:16)</option>
            </select>
          </div>
          <div>
            <label>Escala Texto</label>
            <input type="range" id="sizeSlider" min="50" max="160" value="95" style="width: 100%;">
          </div>
        </div>

        <button id="generateBtn" class="btn-primary">DICTAR SENTENCIA</button>
        <button id="variantBtn" class="btn-variant" style="display:none;">INVOCAR OTRO ÁNGULO</button>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-top: 15px;">
          <button id="downloadBtn" style="background: #0a0a0a; color: #fff; border: 1px solid #222; padding: 14px; border-radius: 12px; cursor: pointer; font-weight: 600;">Guardar</button>
          <button id="shareBtn" style="background: #0a0a0a; color: #fff; border: 1px solid #222; padding: 14px; border-radius: 12px; cursor: pointer; font-weight: 600;">Compartir</button>
        </div>
      </aside>

      <main class="preview-panel">
        <canvas id="mainCanvas"></canvas>
      </main>
    </div>
  </div>

  <script is:inline>
    function initApp() {
      const canvas = document.getElementById('mainCanvas');
      if (!canvas) return;
      const ctx = canvas.getContext('2d');
      
      const ui = {
        input: document.getElementById('userInput'),
        format: document.getElementById('formatSelect'),
        size: document.getElementById('sizeSlider'),
        generateBtn: document.getElementById('generateBtn'),
        variantBtn: document.getElementById('variantBtn'),
        downloadBtn: document.getElementById('downloadBtn'),
        shareBtn: document.getElementById('shareBtn')
      };

      let currentText = "CONFIESA TU BLOQUEO PARA RECIBIR LA SENTENCIA";
      let sentenceID = "SENTENCIA-ID: 00000000";

      function wrapText(context, text, x, y, maxWidth, lineHeight, center) {
        const words = text.split(' ');
        let lines = [];
        let currentLine = '';

        words.forEach(word => {
          const testLine = currentLine + word + ' ';
          if (context.measureText(testLine).width > maxWidth) {
            lines.push(currentLine);
            currentLine = word + ' ';
          } else {
            currentLine = testLine;
          }
        });
        lines.push(currentLine);

        let startY = center ? y - ((lines.length - 1) * lineHeight) / 2 : y;
        lines.forEach(line => {
          context.fillText(line.trim(), x, startY);
          startY += lineHeight;
        });
      }

      function render() {
        const isStory = ui.format.value === 'story';
        const w = 1080;
        const h = isStory ? 1920 : 1080;
        canvas.width = w;
        canvas.height = h;

        // 1. Fondo Base
        ctx.fillStyle = '#000';
        ctx.fillRect(0, 0, w, h);
        
        // 2. Gradiente de atmósfera
        const grad = ctx.createRadialGradient(w/2, h/2, 0, w/2, h/2, w*0.8);
        grad.addColorStop(0, '#0f0f0f');
        grad.addColorStop(1, '#000');
        ctx.fillStyle = grad;
        ctx.fillRect(0, 0, w, h);

        // 3. Texto Principal (Sentencia)
        const fontSize = parseInt(ui.size.value);
        ctx.font = `900 ${fontSize}px 'Bebas Neue'`;
        ctx.textAlign = 'center';
        ctx.fillStyle = '#fff';
        ctx.shadowColor = 'rgba(255, 191, 0, 0.35)';
        ctx.shadowBlur = 45;
        
        wrapText(ctx, currentText.toUpperCase(), w/2, h/2, w * 0.82, fontSize * 1.3, true);

        // 4. --- MARCAS DE AGUA PRO ---
        ctx.shadowBlur = 0;

        // ID de sentencia (Arriba Derecha)
        ctx.font = "400 16px 'Inter'";
        ctx.fillStyle = "rgba(255, 215, 0, 0.4)";
        ctx.textAlign = "right";
        ctx.letterSpacing = "2px";
        ctx.fillText(sentenceID, w - 80, 80);

        // Firma Antonov (Abajo)
        ctx.font = "900 24px 'Inter'";
        ctx.fillStyle = "#ffd700";
        ctx.textAlign = "center";
        ctx.letterSpacing = "4px";
        ctx.fillText("ANTONOV PRO • SISTEMA DE EXCELENCIA", w/2, h - 80);

        // Marca Vertical Lateral
        ctx.save();
        ctx.translate(60, h/2);
        ctx.rotate(-Math.PI/2);
        ctx.font = "800 14px 'Inter'";
        ctx.fillStyle = "rgba(255, 255, 255, 0.12)";
        ctx.textAlign = "center";
        ctx.letterSpacing = "3px";
        ctx.fillText("LA CONSCIENCIA NO TIENE FILTROS • EST. 2024", 0, 0);
        ctx.restore();

        // 5. Ruido Estático Analógico
        for(let i=0; i<6000; i++) {
          const rx = Math.random() * w;
          const ry = Math.random() * h;
          ctx.fillStyle = `rgba(255,255,255,${Math.random() * 0.035})`;
          ctx.fillRect(rx, ry, 1, 1);
        }
      }

      async function callAntonov(isVariation = false) {
  const text = ui.input.value.trim();
  if (!text) return alert("Dime qué pasa, compa. Te escucho.");

  const btn = isVariation ? ui.variantBtn : ui.generateBtn;
  btn.textContent = "ANALIZANDO...";
  btn.disabled = true;

  try {
    const response = await fetch('https://mis-frases.poderparatriunfar.workers.dev/', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        prompt: `Antonov, olvida la poesía y la filosofía barata. Responde como un psicólogo experto y un amigo cercano que habla SENCILLO.
        
        CONTEXTO: "${text}"
        
        TU MISIÓN: Da un consejo basado en la psicología del comportamiento y la sabiduría práctica.
        
        REGLAS DE ORO:
        - LENGUAJE HUMANO: Habla como hablamos tú y yo. Sin palabras rebuscadas.
        - CERCANÍA: Que el usuario sienta que lo entiendes de verdad, no que le estás dando un sermón.
        - DIRECTO AL GRANO: No adornes la verdad. Si el problema es el miedo, habla del miedo con claridad.
        - FORMATO: Una sola frase corta (máximo 12-15 palabras) en MAYÚSCULAS.
        
        ESTILO: "Menos oráculo, más mentor de confianza".
        
        ${isVariation ? 'Dámelo desde otro ángulo, pero igual de sencillo y claro.' : ''}`
      })
    });
    
    const data = await response.json();
    if (data.text) {
      currentText = data.text.replace(/\*/g, ''); // Limpiar asteriscos si la IA los pone
      sentenceID = `SENTENCIA-ID: ${Math.random().toString(36).toUpperCase().substring(2, 10)}`;
      render();
      ui.variantBtn.style.display = 'block';
    }
  } catch (e) {
    alert("Se cayó la conexión, intentemos de nuevo.");
  } finally {
    btn.textContent = isVariation ? "VER OTRA OPCIÓN" : "DICTAR SENTENCIA";
    btn.disabled = false;
  }
}

      ui.generateBtn.onclick = () => callAntonov(false);
      ui.variantBtn.onclick = () => callAntonov(true);
      ui.size.oninput = render;
      ui.format.onchange = render;
      ui.downloadBtn.onclick = () => {
        const a = document.createElement('a');
        a.download = `antonov-${Date.now()}.png`;
        a.href = canvas.toDataURL();
        a.click();
      };

      render();
    }

    document.addEventListener('DOMContentLoaded', initApp);
    document.addEventListener('astro:page-load', initApp);
  </script>
</Layout>
